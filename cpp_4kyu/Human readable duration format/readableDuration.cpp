#include <string>

std::string format_duration(int seconds) {
    int amountValues = seconds;
    while (/* condition */)
    {
        /* code */
    }
    
}

#include <iostream>

int main() {
    auto result714 = format_duration(714);
    std::cout << result714 << std::endl;

    auto result714 = format_duration(3665);
    std::cout << result714 << std::endl;

    return 0;
}